# Pinned exactly so a Railway rebuild of unchanged source cannot pick up a new
# major version. Previously `pandas>=2.0.0` resolved to pandas 3.x and
# `streamlit>=1.28.0` to 1.62, so two deploys of the same commit could differ.
# Bump these deliberately, and test before committing the bump.
streamlit==1.62.0
pandas==2.3.3
SQLAlchemy==2.0.52
psycopg2-binary==2.9.12
fpdf2==2.8.8
